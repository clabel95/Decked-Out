[
    {
        "sideA": "when should you set a variable to 'null'",
        "sideB": "When you expect it to contain a reference to an object. That way, you can explicitly check for the value null to see if the var has been filled with an object reference."  
    },
    {
        "sideA": "What is a 'unary' operator?",
        "sideB": "One that only operates on one value, like ++ or -- as in ++var."        
    },
    {
        "sideA": "How do the constructor, prototype, and instances refer to one another in the prototype pattern?",
        "sideB": "Constructor has a property called prototype, which is an object. Prototype has a property called constructor that points back at the constructor. Each instance has a property called prototype which points to the prototype object."        
    },
    {
        "sideA": "When is try-catch statement most useful?",
        "sideB": "For handling an error that is out of your control — for example, when dealing with a library whose code you can't touch."        
    },
    {
        "sideA": "Is React declarative or imperative?",
        "sideB": "It's declarative. You say what you want to have happen rather than give step by step instructions."        
    },
    {
        "sideA": "Does case matter in naming React components?",
        "sideB": "Yes, they must be uppercase or React will treat them as DOM tags."        
    },
    {
        "sideA": "Why is the index from an array not a good key for React?",
        "sideB": "If order changes, performance can suffer."        
    },
    {
        "sideA": "What is function declaration hoisting?",
        "sideB": "Before code begins running, the javascript engine 'hoists' functions to the top of the execution context. That way, a function can be called even if it doesn't appear in code until later."
    },
    {
        "sideA": "What is the default type for a button element in a form?",
        "sideB": "Submit."        
    },
    {
        "sideA": "What is JSON parse() method?",
        "sideB": "JSON.parse(someJSON) will turn the JSON into a javascript object."
    },
    {
        "sideA": "What is a 'controlled component'?",
        "sideB": "An input form where React controls state, e.g. input value = {this.state.value}."        
    },
    {
        "sideA": "Asynchronous setState() syntax?",
        "sideB": "this.setState((prevState, props) => ({counter: prevState.counter + props.increment}));"
        
    },
    {
        "sideA": "What are the string regular expression methods?",
        "sideB": "match() search(); replace(); split();"
    },
    {
        "sideA": "How can you see if one node contains another?",
        "sideB": "someNode.contains( otherNode )."
    }
]